{
  "blocked_commands": [
    "rm -rf",
    "sudo rm",
    "dd if=",
    "mkfs",
    "format",
    "> /dev/",
    "chmod 777",
    "curl -s | sh",
    "wget -O - | sh"
  ],
  "error_messages": {
    "rm -rf": "危険なファイル削除コマンドは実行できません",
    "sudo rm": "管理者権限でのファイル削除は実行できません", 
    "dd if=": "ディスク操作コマンドは実行できません",
    "mkfs": "ファイルシステム作成コマンドは実行できません",
    "format": "フォーマットコマンドは実行できません",
    "> /dev/": "デバイスファイルへの書き込みは実行できません",
    "chmod 777": "全権限付与は実行できません",
    "curl -s | sh": "外部スクリプトの直接実行は実行できません",
    "wget -O - | sh": "外部スクリプトの直接実行は実行できません",
    "default": "このコマンドは実行が制限されています"
  }
}